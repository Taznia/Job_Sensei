.dart_tool/
.packages
.pub/
.build/
coverage/
.idea/
# Ignore local VS Code settings but keep the shared run configuration, so the
# play button works on a fresh clone without picking a device first.
.vscode/*
!.vscode/launch.json
*.iml
*.ipr
*.iws
build/
android/local.properties
**/ephemeral/
*.apk
.env
**/DerivedData/
**/Pods/
**/xcuserdata/
.flutter-plugins
.flutter-plugins-dependencies

# Figma tooling — token file is secret, pulled output is regenerable
.figma.env
tool/figma_out/
